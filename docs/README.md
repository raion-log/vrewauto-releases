# 수강생 안내 사이트

GitHub Pages 로 서비스한다. **주소·소스·수정 권한이 전부 `raion-log` 계정에 있다.**
2026-09-06 이전 담당자 계정 Netlify 에서 옮겨 왔고, 2026-09-19 판이 하나가 되면서 페이지도 하나로 합쳤다.

주소: https://raion-log.github.io/vrewauto-releases/

```
docs/
  index.html      설치 안내  ← update/with-capcut.json 을 읽는다
  with-capcut/    옛 주소 → 루트로 이동 (즐겨찾기 보호)
  v210/           옛 주소 → 루트로 이동
  sayong/  flow/  사용법·흐름 안내
  img/            아이콘
```

## 판 번호와 다운로드 주소를 고치는 법

**HTML 을 고치지 않는다.** 페이지가 `update/with-capcut.json` 을 읽어 판 번호·다운로드 주소·
파일 이름을 그린다. 채널 파일만 고치고 push 하면 페이지가 따라 바뀐다.

- 설치파일 주소가 **비어 있으면 페이지가 덮어쓰지 않는다.** 판 번호도 그대로 둔다 —
  버튼이 받는 파일과 표시가 어긋나지 않게
- 채널을 못 읽어도 HTML 에 적힌 기본값으로 계속 동작한다 (2026-08-12 채널 차단 전례)
- 구글 드라이브 경고 단계는 다운로드 버튼이 드라이브를 가리킬 때만 보인다. 배포 저장소
  릴리즈 주소가 채워지면 저절로 숨고 뒤 단계 번호가 다시 매겨진다
- ★`version` 은 설치파일을 릴리즈에 올리고 안내를 보낸 **뒤에** 올린다

## 지금 HTML 기본값에 대해

HTML 에 적힌 기본 다운로드 링크는 아직 **이전 담당자 계정 구글 드라이브**다(채널이 비어 있어서).
새 판을 배포하며 채널을 채우면 쓰이지 않는다. 그 계정을 지우기 전에 반드시 채널을 채운다.
